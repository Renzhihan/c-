11.13
列表初始化
  int u = 0;
	int u = { 0 };
	int u{ 0 };
	int u(0);
定义在函数体外的变量初始化为0，函数体内的不被初始化。
声明与定义：
  extern int i;//声明
  extern i=1;//定义
  int i;/声明并定义
  一个变量只能被定义一次，但可声明多次。
标识符由字母，数字，下划线组成，必须由数字或字母开头
复合类型(compound type):基于其他类型定义的类型   c++包括：指针和引用
  引用(reference)
    	int ival = 1;
	    int& refVal = ival;
	    int& refVal2;//报错，引用必须初始化
    引用只是为一个已经存在的对象所起的另外一个名字。
  指针(pointer)
      空指针
        int *p1=nullptr;
      赋值永远改变等号左边的值
      void*:特殊类型的指针类型，可用于存放任意对象的地址。
      从右向左阅读，离变量名最近的符号对变量的类型有最直接的影响。
const限定符
  
